NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=dev-secret-change-me

# SMTP settings for passwordless magic links (NextAuth Email provider)
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-smtp-username
EMAIL_SERVER_PASSWORD=your-smtp-password
# Set to true only if your SMTP supports TLS on the chosen port (e.g., 465)
EMAIL_SERVER_SECURE=false
EMAIL_FROM=info@yourexitplans.com

# n8n Webhook URL for idea validation form (used server-side)
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/idea-validation

# Google Analytics
NEXT_PUBLIC_GA_ID=G-4YSQPT6D0J

# Upstash Redis for Rate Limiting (get from https://console.upstash.com/)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token-here

# For Postie (local testing), you can use:
# EMAIL_SERVER_HOST=localhost
# EMAIL_SERVER_PORT=2525
# EMAIL_SERVER_USER=
# EMAIL_SERVER_PASSWORD=
# EMAIL_SERVER_SECURE=false
# EMAIL_FROM=login@localhost.test
